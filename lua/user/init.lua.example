-- ~/.config/nvim/lua/user/init.lua — your personal NyanVim overrides.
-- This file is git-ignored: `:NyanUpdate` never touches it.
-- Open it any time with :NyanConfig (or <Space>nc).
--
-- Extra plugins go in lua/user/plugins/*.lua as normal lazy.nvim specs:
--   return { { "owner/repo", opts = {} } }

-- Options
-- vim.opt.relativenumber = false
-- vim.opt.wrap = true

-- Keymaps
-- vim.keymap.set("n", "<leader>x", "<cmd>echo 'hi'<cr>", { desc = "My map" })

-- Theme: pick with <Space>th; or force one here
-- vim.cmd.colorscheme("nightcity-lucy")

-- Nag when a motion key is held ("Hold it Cowboy!")
-- vim.g.nyanvim_cowboy = true

-- Editor-style shortcuts, if your fingers come from VS Code / JetBrains
-- (they shadow Vim's C-p / C-f / C-e / C-b defaults, which is why they are off)
-- vim.keymap.set("n", "<C-p>", "<cmd>FzfLua files<cr>", { desc = "Search files" })
-- vim.keymap.set("n", "<C-S-f>", "<cmd>FzfLua live_grep<cr>", { desc = "Search in project" })
